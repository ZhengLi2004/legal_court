"""A package for data loading and schema definitions.

This package contains modules related to the handling of legal case data. It
includes:
-   `schema.py`: Defines the Pydantic model (`CaseData`) for a structured
    representation of a legal case.
-   `loader.py`: Provides the `CaseDataLoader` class for reading, parsing, and
    validating raw case data from files.
"""
